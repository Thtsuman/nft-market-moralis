import { v4 as uuid } from 'uuid';

export const pages = [{
    name: 'Explore',
    link: '/nft-marketplace',
    id: uuid()
}];

export const settings = [{
    text: 'Logout',
}];

export const logoObj = {
    text: 'NFT',
    link: "/"
}